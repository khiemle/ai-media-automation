name: Frontend Tests

on:
  pull_request:
    paths:
      - 'console/frontend/**'
      - '.github/workflows/frontend.yml'
  push:
    branches: [main]
    paths:
      - 'console/frontend/**'
      - '.github/workflows/frontend.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: console/frontend
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: console/frontend/package-lock.json

      - name: Install dependencies
        # npm install (not npm ci) because the lockfile is generated on macOS
        # and CI's Linux runner needs platform-specific optional deps that
        # npm ci would reject as "missing from lock file".
        run: npm install --no-audit --no-fund

      - name: Build
        run: npm run build

      - name: Run tests
        run: npm test
